# UI 设计系统优先

- `docs/ui-system.html` 是本项目 UI 的权威基准，适用于布局、颜色、文字层级、图标、地图表现、组件状态和交互反馈。
- **任何 UI 修改，先更新设计系统网页中的规范和对应示例，再修改游戏原型。** 不允许先改原型、以后再补设计系统。无需为这一步额外请求用户确认。
- 状态组件样本在 `docs/ui-components.html`，建筑与地图示例在 `docs/building-tiles.js`。新增状态或交互必须有可检查的示例和说明。页面保持精简，只介绍核心设计规范与必要示例，不加入完整原型、设计参考历史或重复流程说明。
- 优先复用 `docs/color-tokens.css`、`game.css`、`game-palette.css`、`tile-system.css/js`、`road-system.js` 及图标目录，避免为设计系统另画一套与原型脱节的组件。
- 完成后对照设计系统和原型验证桌面、窄屏、相关交互及状态。组件示例不能读写玩家进度。
- 如发现原型与设计系统不一致，先记录并同步设计系统，再进行本次 UI 修改；用户明确要求的设计变更优先。
