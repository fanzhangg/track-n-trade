(function () {
 'use strict';
 const {catalog,icon}=window.TradeIcons;
 const section=document.createElement('section');section.id='icons';section.className='icon-system';
 section.innerHTML=`<div class="section-heading"><div><h2>图标</h2></div></div>
 <p class="icon-intro">建筑与地形用彩色微缩插画，资源用固定主色与独立轮廓的圆润微缩物件，工人与居民用 meeple，操作与状态用墨蓝 SVG。</p>
 <div class="icon-review-controls"><label>尺寸 <select id="icon-size"><option value="64">64 px · 详情 / 图鉴</option><option value="48">48 px · 地图 / 建造</option><option value="32">32 px · 紧凑列表</option><option value="24">24 px · 资源</option><option value="20">20 px · 价格</option><option value="16">16 px · 最小检查</option></select></label><label>背景 <select id="icon-ground"><option value="surface-panel">纸白面板</option><option value="terrain-grass">草地</option><option value="terrain-forest">森林</option><option value="terrain-rock">岩地</option><option value="terrain-lake">湖泊</option></select></label><label><input type="checkbox" id="icon-gray">灰度辨识</label><label>查找 <input id="icon-search" type="search" placeholder="名称或 ID" aria-label="查找图标"></label></div>
 <p class="feedback" id="icon-size-note">64 px 展示完整生产设施，无角标。点击任意图标查看用途和重绘来源。</p>
 <div id="resource-size-samples" class="resource-size-samples"></div><div class="icon-review" id="icon-review"></div>
 <aside class="icon-inspector" id="icon-inspector" aria-live="polite"></aside>
 <ul class="icon-rules"><li><b>尺寸：</b>建筑使用 48 / 64px，角色在地图使用 17px、面板使用 22px；货物使用 16–32px，16–24px 操作符号使用 SVG。</li><li><b>样式：</b>按透明边界统一视觉大小，最长边占框 82%。建筑与地形统一左上受光、简洁切面与无硬描边风格；meeple 用同样的低多边形材质表达体积。不使用产物角标，不把棋子包进 UI 卡片。</li><li><b>辨识：</b>灰度模式用于检查轮廓。工人与居民轮廓相同是有意的角色语法，职能由位置、上下文、名称和颜色共同表达。</li><li><b>可访问性：</b>独立图标有可访问名称；旁边已有文字时图像作为装饰。锁定和异常必须附原因，纯图标按钮必须有名称。</li></ul>
 <p class="icon-license"><a href="../assets/icons/v1/README.md">素材规范与许可 ↗</a></p>`;
 document.querySelector('main>footer').before(section);
 const samples=document.getElementById('resource-size-samples');
 samples.innerHTML='<h3>资源 · 实际尺寸与价格胶囊</h3>'+catalog.filter(i=>i.group==='goods').map(i=>`<div class="resource-sample"><div class="resource-sample-head"><b>${i.name}</b>${icon(i.id,{size:64})}</div><div class="resource-sizes">${[16,20,24,32].map(size=>`<span>${icon(i.id,{size})}<small>${size}px</small></span>`).join('')}</div><span class="resource-price">${icon(i.id,{size:20})} › ${icon('coin',{size:20})} 234 /件</span><span class="resource-silhouette" title="纯黑剪影">${icon(i.id,{size:24})}</span></div>`).join('');
 const review=document.getElementById('icon-review');let selected='sawmill';
 const headings={building:'建筑 · 6 座生产设施 + 城镇',people:'角色 · 落在板块上的 meeple',goods:'货物 · 10 种圆润微缩物件',economy:'经济 · 唯一全局货币',world:'地形、工具与运输科技',control:'操作与状态 · 同一套实心符号'};
 function inspect(id){selected=id;const item=catalog.find(i=>i.id===id);const ids=[id,...(item.badge?[item.badge]:[]),...(item.compact&&item.compact!==id?[item.compact]:[])];const sources=[...new Set(ids)].map(key=>{const s=window.TradeIconSources.assets.find(a=>a.id===key);return s?`<p><b>${catalog.find(i=>i.id===key)?.name||key}</b> · ${s.pack} · ${Math.round(s.width)}×${Math.round(s.height)}<br><small>${s.source}</small></p>`:'';}).join('');document.getElementById('icon-inspector').innerHTML=`<h3>${item.name} <small>／ ${id}</small></h3><p>${item.use}。辨识线索：${item.cue}。</p>`;review.querySelectorAll('[data-icon-id]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.iconId===id)));}
 function render(){const size=+document.getElementById('icon-size').value,q=document.getElementById('icon-search').value.trim().toLowerCase();let count=0;review.innerHTML=Object.entries(headings).map(([group,title])=>{const items=catalog.filter(i=>i.group===group&&(i.name+i.id+i.use).toLowerCase().includes(q));count+=items.length;return items.length?`<h3 class="icon-subheading">${title}</h3><div class="${group==='control'?'icon-control-grid':'icon-grid'}">${items.map(i=>`<button class="icon-tile" data-icon-id="${i.id}" aria-pressed="${i.id===selected}"><span class="icon-stage">${icon(i.id,{size:group==='control'?24:size,decorative:true})}</span><span class="icon-tile-copy"><strong>${i.name}</strong><code>${i.id}</code><small>${group==='control'?'':i.cue}</small></span></button>`).join('')}</div>`:'';}).join('');if(!count)review.innerHTML='<p class="icon-empty" role="status">没有匹配图标，请尝试其他名称。</p>';document.getElementById('icon-size-note').textContent=`${size} px：建筑显示完整生产设施，无产物角标；名称辅助识别。操作图标保持 24 px。`;}
 review.addEventListener('click',e=>{const b=e.target.closest('[data-icon-id]');if(b)inspect(b.dataset.iconId);});
 document.getElementById('icon-size').addEventListener('change',render);document.getElementById('icon-search').addEventListener('input',render);
 document.getElementById('icon-ground').addEventListener('change',e=>section.style.setProperty('--review-bg',`var(--${e.target.value})`));
 document.getElementById('icon-gray').addEventListener('change',e=>{review.classList.toggle('grayscale',e.target.checked);samples.classList.toggle('grayscale',e.target.checked);});render();inspect(selected);
})();
